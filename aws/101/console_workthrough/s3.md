## 1차 시도

### General configuration

1. AWS Region
리전 옵션.
s3는 리전 옵션이기 때문에 자신이 어느 리전인지 주의하면 된다.

2. Bucket namespace
글로벌 이름과 계정 리전별 이름으로 선택이 가능하다.
s3의 경우 api call?로 부를 수 있어 이론 상 무관한 사람이 s3에 요청이 가능하다.
과거에는 우연히 똑같은 s3이름이 생겼던 경우가 있어, 조회요청이 너무 많아 이상한 과금이 생긴 겨웅가 있다.

3. Copy settings from existing bucket - optional
기존에 존재하는 세팅을 가져오는 옵션
정확이 어떤 결과가 나오는지는 모르겠다.

### Object Ownership
1. Object Ownership
acl을 설정하는 옵션
acl의 경우는 sg와는 다르게 세세하게 설정이 가능하지만 건망증이 심한 보안관에 비유된다.
stateless에 해당하고 인바운드/아웃바운드를 전부 설정한다.
왜 acl을 s3에 권장하지 않는 것은 모르나, 거의 대부분의 sa가 sg만 사용해보았다고 한다.
sg는 statefull하기 때문이기도 하고, 더 큰 범위의 보안을 책임지기? 때문이다.

2. Object Ownership
acl을 사용하면 나오는 옵션
권장되지도 않아서 뭔 지 모르겠다.

### Block Public Access settings for this bucket

1. Block all public access
s3는 저장소이기에 기본적으로 인터넷의 접근을 전부 막는 것을 권장하는 것으로 생각된다.\

### Bucket Versioning

1. Bucket Versioning
버킷 안에 저장된 데이터를 버전별로 관리하는 옵션.
삭제를 해도 삭제 마커가 남는 것이고 실제로 삭제되기 까지 기간이 걸린다.
그 외에도 데이터가 변경되도 기록을 유지한다.
이로 인해 요금이 조금 더 부가되는 것으로 알지만 안정적인 운영이 가능해진다.

### Tags - optional

더 말은 필요 없다.

### Default encryption

1. Encryption type
잘 모르겠다.
서버에서 암호를 걸어주는 옵션인 것은 아는데, s3와 kms의 차이점을 모르겟다.
kms는 비밀번호 전체를 관리해주는 옵션이고 주기적인 변경도 가능한 것으로 안다.
듀얼-레이어는 더 모르겠다.


---

## Kiro Grade: B-

### Per-section feedback

**1. AWS Region** ✅ Correct.
- S3 buckets are regional. Good to note.
- One addition: once created, you can't change a bucket's region.

**2. Bucket namespace** ⚠️ Partially correct, some confusion.
- Two options are: globally unique name (default, traditional) vs account+region scoped name (newer option).
- "api call로 부를 수 있어 무관한 사람이 s3에 요청이 가능하다" — this is about the old global namespace where anyone could guess your bucket name and send requests. The newer account-scoped namespace helps prevent this.
- The billing incident you mentioned is a real thing — correct. Third parties could make requests to your bucket URL and you'd get charged for the denied requests. AWS has since addressed this.

**3. Copy settings from existing bucket** ✅ Correct concept.
- It copies settings (encryption, versioning, public access, etc.) from an existing bucket to pre-fill the form. It does NOT copy the bucket policy. Console-only feature (not available in CLI).

**4. Object Ownership / ACL** ❌ Major confusion.
- You're mixing up S3 ACLs with VPC Network ACLs (NACLs). They are completely different things:
  - **S3 ACL** = access control for S3 objects/buckets (who can read/write objects). Legacy feature.
  - **VPC NACL** = network-level firewall for subnets (stateless, inbound/outbound rules). This is what you described.
  - **Security Group** = instance-level firewall (stateful).
- S3 Object Ownership has 3 options:
  - **Bucket owner enforced** (default, recommended) — ACLs disabled, bucket owner owns everything, use policies for access control
  - **Bucket owner preferred** — ACLs enabled, bucket owner gets ownership if uploader uses `bucket-owner-full-control` ACL
  - **Object writer** — ACLs enabled, the uploader owns the object
- AWS recommends keeping ACLs disabled (bucket owner enforced) because bucket policies are simpler and more flexible.

**5. Block Public Access** ✅ Correct.
- Default is all 4 settings blocked. Recommended to keep all enabled unless you specifically need public access (e.g. static website hosting).

**6. Bucket Versioning** ✅ Mostly correct.
- Delete marker concept is correct.
- Previous versions are kept and do incur storage costs — correct.
- Minor clarification: deleted objects aren't "eventually deleted" automatically. The delete marker hides the object, but old versions remain forever unless you set up a Lifecycle policy to clean them up.

**7. Tags** ✅ Fine.

**8. Default encryption** ⚠️ Incomplete but honest.
- Three options:
  - **SSE-S3** (default) — AWS manages the keys entirely. Free. No extra config needed.
  - **SSE-KMS** — AWS KMS manages the keys. You can use the AWS-managed key (`aws/s3`, free) or your own customer-managed key (costs per API call). Gives you key rotation, audit trail via CloudTrail, and fine-grained access control.
  - **DSSE-KMS** — Double encryption with two separate KMS keys. For highly regulated workloads (e.g. CNSSP 15 compliance). Rarely needed.
- The key difference: SSE-S3 = simple/free, SSE-KMS = more control + audit + cost.

### Missing console options
- **Advanced settings → S3 Object Lock** — prevents objects from being deleted or overwritten for a retention period. Automatically enables versioning. Used for compliance (WORM — Write Once Read Many).


## 2차 시도

### General configuration

1. AWS Region
리전 옵션.
s3는 리전 옵션이기 때문에 자신이 어느 리전인지 주의하면 된다.
한 번 만들면 바꿀 수 없다.

2. Bucket namespace
글로벌 옵션과 계정 리전별 네임을 선택할 수 있다.
바뀐 것은 아는데 왜 바뀐 것인지, 바뀌어서 좋은 이점이 무엇인지는 잘 모르겠다.
지역적인 이름으로 할당할 시에는 버킷네임(사용자 설정)뒤에 지역 접미사가 붙는다.

3. Copy settings from existing bucket - optional
콘솔에서만 사용이 가능한 옵션.
기존 버킷의 옵션에서 그대로 복사한다.
policy 같은 경우는 복사하지 못한다.

### Object Ownership
1. Object Ownership
acl 사용을 선택하는 옵션이다.
acl을 사용하지 않는 경우 소유자만 접근이 가능한 것으로 보인다.
사용하는 경우에는 객체를 쓴 사람에게 소유권이 있다.
이 경우에는 어떤 차이점이 존재하는 지 모르겠다.

### Block Public Access settings for this bucket

1. Block all public access
퍼블릭에서 접근이 가능한 지를 설정하는 옵션
일반적인 경우에는 s3에 접근하는 데이터가 모두 프라이빗이니 접근을 막는다.
정적 웹사이트 호스팅의 경우에 허가하면 되고, 이 경우에도 최소 권한 원칙에 맞게 설정하면 된다.

### Bucket Versioning

1. Bucket Versioning
버져닝을 선택하는 옵션.
버저닝을 선택하지 않으면 데이터가 실수로 삭제되는 경우를 막을 수 없지만,
선택하는 경우에는 삭제를 하더라도 바로 삭제가 되는 것이 아니라 삭제 마커가 붙는 형식으로 바뀐다.
이후 삭제마커가 일정 시간을 초과하면 진짜로 삭제한다.
그 외에도 데이터의 변경이 존재하는 경우에도 몇 가지 버전을 동시에 관리한다.

### Tags - optional

더 말할 필요 없다.

### Default encryption

1. Encryption type
암호화 타입을 결정하는 옵션
s3와 kms에게 맡기는 방법은 서버쪽이며,
kms와 듀얼레이어를 사용하는 방식이 존재한다.
각각의 방식이 어떤 차이점이 있는 지 모르겠다.

### Advanced settings

skip

## Codex Grade: B- (75/100)

2차 시도는 1차보다 확실히 좋아졌다.

- Region, public access, copy settings, versioning의 큰 방향은 잘 잡았다.
- 하지만 `Object Ownership`, `Default encryption`, `Advanced settings`는 아직 AWS 101 발표용으로는 설명이 부족하다.
- 특히 `잘 모르겠다`라고 남긴 부분은 AWS가 왜 그런 옵션을 두는지까지 연결해서 이해해야 한다.

#### 총평

- 현재 수준은 `공부 메모로는 괜찮음`
- 그러나 `AWS 101 발표/고객 설명용으로는 한 번 더 다듬어야 함`
- 가장 먼저 보완해야 할 것은 아래 4개다.

1. `Object Ownership와 ACL의 관계`
2. `Versioning에서 delete marker가 실제로 무엇인지`
3. `SSE-S3 / SSE-KMS / DSSE-KMS 차이`
4. `Advanced settings의 Object Lock`

#### 잘 모르겠다를 풀어서 설명

##### Bucket namespace

- 네가 쓴 `바뀐 것은 아는데 왜 바뀐 것인지`의 핵심은, 버킷 이름이 S3 요청 경로와 endpoint에서 중요하기 때문이다.
- 전통적인 general purpose bucket은 이름이 매우 넓은 범위에서 유일해야 해서 충돌과 예측 가능성 문제가 있었다.
- 그래서 newer bucket type이나 scoped naming 방식은 이름 충돌이나 request routing 문제를 줄이는 방향으로 이해하면 된다.
- AWS 101 수준에서는 `버킷 이름은 단순 표시용이 아니라 실제 요청 경로에 영향을 주기 때문에 중요하다` 정도로 말하면 충분하다.

##### Object Ownership

- 네가 쓴 `acl을 사용하지 않는 경우 소유자만 접근이 가능한 것으로 보인다`는 절반만 맞다.
- 더 정확히는 `Bucket owner enforced`에서는 ACL을 아예 끄고, bucket owner가 객체를 소유하며 access control은 bucket policy/IAM policy로 관리한다.
- `Bucket owner preferred`는 ACL을 쓰되 bucket owner가 소유권을 가져오도록 유도하는 방식이다.
- `Object writer`는 객체를 업로드한 쪽이 객체 소유권을 가진다.
- AWS가 `Bucket owner enforced`를 권장하는 이유는 ACL보다 policy 기반 관리가 단순하고 일관되기 때문이다.

##### Bucket Versioning

- 네가 쓴 `삭제마커가 일정 시간을 초과하면 진짜로 삭제한다`는 부분은 틀렸다.
- delete marker는 자동 만료 개념이 아니다.
- versioning이 켜져 있으면 삭제 요청 시 최신 버전 앞에 delete marker가 붙어서 보이지 않게 만들 뿐이고, 이전 버전은 계속 남아 있다.
- 오래된 버전을 자동 정리하려면 `Lifecycle rule`을 따로 설정해야 한다.

##### Default encryption

- 네가 쓴 `s3와 kms에게 맡기는 방법`은 방향은 맞다.
- 더 정확히는 아래 3개로 정리하면 된다.
- `SSE-S3`
  S3가 키를 관리한다. 가장 간단하고 기본적인 서버 측 암호화다.
- `SSE-KMS`
  AWS KMS 키를 사용한다. key policy, audit trail, rotation, 권한 제어가 더 강하다.
- `DSSE-KMS`
  KMS 기반 이중 계층 암호화다. 더 높은 규제/보안 요구가 있을 때 사용한다.
- AWS 101 수준에서는 `SSE-S3는 간단함`, `SSE-KMS는 제어와 감사가 강함`, `DSSE-KMS는 특수한 compliance용` 정도로 기억하면 충분하다.

##### Advanced settings

- `skip`했지만 최소한 `S3 Object Lock`은 알아야 한다.
- Object Lock은 객체를 일정 기간 삭제/수정하지 못하게 막는 기능이다.
- 보통 compliance, archive, legal hold, WORM(Write Once Read Many) 요구가 있을 때 사용한다.
- 일반적인 버킷에는 꼭 필요한 것은 아니지만, 콘솔 옵션으로 존재한다는 점은 알아두는 것이 좋다.

#### 발표용으로 바로 쓸 수 있게 고치면 좋은 표현

##### Object Ownership

- `이 옵션은 ACL을 사용할지와 업로드된 객체의 소유권을 누구에게 둘지 정하는 설정이다. 대부분의 경우 ACL을 끄는 Bucket owner enforced가 권장된다.`

##### Bucket Versioning

- `Versioning을 켜면 객체의 이전 버전을 유지할 수 있다. 삭제 시에는 실제 삭제 대신 delete marker가 생기며, 이전 버전은 따로 정리하기 전까지 남아 있다.`

##### Default encryption

- `기본 암호화는 S3가 저장 데이터를 자동으로 암호화하도록 하는 옵션이다. SSE-S3는 가장 단순한 기본 방식이고, SSE-KMS는 키 제어와 감사 기능이 더 필요한 경우에 사용한다.`

##### Advanced settings

- `Advanced settings에는 S3 Object Lock 같은 특수 보존 기능이 있다. 주로 compliance나 장기 보관 요구가 있을 때 사용한다.`

#### Terminology

- `S3` = `Simple Storage Service`
- `ACL` = `Access Control List`
- `IAM` = `Identity and Access Management`
- `KMS` = `Key Management Service`
- `SSE` = `Server-Side Encryption`
- `SSE-S3` = `Server-Side Encryption with Amazon S3 managed keys`
- `SSE-KMS` = `Server-Side Encryption with AWS Key Management Service keys`
- `DSSE-KMS` = `Dual-Layer Server-Side Encryption with AWS KMS keys`
- `WORM` = `Write Once Read Many`

#### AWS 101 수준에서 추가로 자주 나오는 질문

- S3와 EBS는 어떻게 다른가?
- bucket과 object는 어떻게 다른가?
- versioning을 켜면 삭제가 어떻게 동작하는가?
- bucket policy와 ACL은 어떻게 다른가?
- public bucket은 언제 필요한가?
- S3 정적 웹사이트 호스팅은 언제 쓰는가?
- S3와 CloudFront는 어떻게 같이 쓰는가?
- Glacier 계열 스토리지 클래스는 언제 쓰는가?
