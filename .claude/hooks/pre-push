#!/usr/bin/env bash
# Claude-owned pre-push hook.
# Shared study-content acknowledgment now lives in .githooks/pre-push-study-content.
# Claude keeps this file so Claude-specific checks can be added later without
# duplicating the shared confirmation flow.

exit 0
