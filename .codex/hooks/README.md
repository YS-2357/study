# .codex/hooks

Codex-specific hook implementations for this repository.

- `README.md`: This file.
- `pre-push`: Codex validation checks that run before `git push`, including a hard stop if `.claude/` changes are present.
